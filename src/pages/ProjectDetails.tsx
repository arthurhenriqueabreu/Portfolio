"use client";
import { Link,useParams } from "react-router-dom";import { ArrowLeft,Check,ExternalLink,Github,Lightbulb,ShieldCheck,Wrench } from "lucide-react";import { getProject } from "@/src/data/projects";import { PixelWindow } from "@/src/components/PixelUI";import NotFound from "./NotFound";
export default function ProjectDetails(){const{slug=""}=useParams();const project=getProject(slug);if(!project)return <NotFound/>;return <main className="project-page"><header className="project-header"><Link to="/#projetos"><ArrowLeft/> Voltar ao corredor</Link><div className="brand"><span className="brand-mark">AH</span><span>HOTEL ARTHUR<small>QUARTO {project.room}</small></span></div></header><section className="project-hero"><div><p className="eyebrow">QUARTO {project.room} • {project.status.toUpperCase()}</p><h1>{project.name}</h1><p>{project.description}</p><div className="tech-chips">{project.technologies.map(t=><span key={t}>{t}</span>)}</div></div><div className={`project-illustration project-${project.room}`} role="img" aria-label={`Ilustração original para ${project.name}`}><span>{project.room}</span></div></section><section className="project-content"><PixelWindow><Lightbulb className="feature-icon"/><p className="window-label">O PROBLEMA</p><h2>O que este projeto resolve</h2><p>{project.problem}</p></PixelWindow><PixelWindow><ShieldCheck className="feature-icon"/><p className="window-label">FUNCIONALIDADES</p><h2>O que foi construído</h2><ul className="check-list">{project.features.map(i=><li key={i}><Check/>{i}</li>)}</ul></PixelWindow><PixelWindow><Wrench className="feature-icon"/><p className="window-label">BASTIDORES</p><h2>Desafios</h2><ul>{project.challenges.map(i=><li key={i}>{i}</li>)}</ul></PixelWindow><PixelWindow><Lightbulb className="feature-icon"/><p className="window-label">APRENDIZADOS</p><h2>O que levei comigo</h2><ul>{project.learnings.map(i=><li key={i}>{i}</li>)}</ul></PixelWindow></section><div className="project-actions"><button disabled><Github/> GitHub <small>link a adicionar</small></button><button disabled><ExternalLink/> Demonstração <small>quando disponível</small></button></div><Link className="pixel-button back-button" to="/#projetos"><ArrowLeft/> Voltar aos projetos</Link></main>}
